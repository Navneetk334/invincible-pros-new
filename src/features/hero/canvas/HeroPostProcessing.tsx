"use client";

// import { useHeroStore } from "../store/use-hero-store";

export default function HeroPostProcessing() {
  // Configured variables for WebGL Post-Processing shaders
  // To be used during styling phase with <EffectComposer>, <Bloom>, etc.
  // const bloomIntensity = useHeroStore((state) => state.bloomIntensity);
  // const chromaticAberrationOffset = useHeroStore((state) => state.chromaticAberrationOffset);

  return null;
}
